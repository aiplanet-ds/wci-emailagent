# ============================================================================
# FRONTEND CONFIGURATION (REQUIRED)
# ============================================================================
# These environment variables must be set when building the frontend.
# They are baked into the frontend at build time (Vite).
#
# Copy this file to .env and fill in the values before running npm run build.
# IMPORTANT: No defaults are used - all values MUST be configured.
# ============================================================================

# Backend API base URL (REQUIRED)
# Development example: http://localhost:8000
# Production example: https://your-production-domain.com
VITE_API_BASE_URL=

# Login URL (REQUIRED)
# Development example: http://localhost:8000/login
# Production example: https://your-production-domain.com/login
VITE_LOGIN_URL=

# Logout URL (REQUIRED)
# Development example: http://localhost:8000/logout
# Production example: https://your-production-domain.com/logout
VITE_LOGOUT_URL=
