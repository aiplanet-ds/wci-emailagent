# ============================================================================
# FRONTEND CONFIGURATION (REQUIRED)
# ============================================================================
# These environment variables must be set when building the frontend.
# They are baked into the frontend at build time (Vite).
#
# Copy this file to .env and fill in the values before running npm run build.
# ============================================================================

# Backend API base URL (REQUIRED)
# For development: http://localhost:8000
# For production: https://your-production-domain.com
VITE_API_BASE_URL=http://localhost:8000

# Login URL (REQUIRED)
# For development: http://localhost:8000/login
# For production: https://your-production-domain.com/login
VITE_LOGIN_URL=http://localhost:8000/login

# Logout URL (REQUIRED)
# For development: http://localhost:8000/logout
# For production: https://your-production-domain.com/logout
VITE_LOGOUT_URL=http://localhost:8000/logout
