# Microsoft Azure AD / App Registration
# AZ_TENANT_ID Options:
#   - "common" = Multi-tenant (allows ANY Microsoft account - Outlook, Hotmail, Office 365, etc.)
#   - "organizations" = Multi-tenant (allows only organizational accounts, no personal accounts)
#   - "consumers" = Only personal Microsoft accounts (Outlook, Hotmail, etc.)
#   - "your-tenant-id" = Single-tenant (only users from your specific Azure AD tenant)
#
# Recommended: Use "common" for maximum flexibility
AZ_TENANT_ID=common
AZ_CLIENT_ID=your-client-id-here
AZ_CLIENT_SECRET=your-client-secret-here
AZ_USER_EMAIL=your-email@domain.com   # only used in application mode

# Authentication Mode: delegated (recommended) | application
GRAPH_MODE=delegated

# Azure OpenAI Configuration
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here
AZURE_OPENAI_API_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4.1
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# LLM Price Change Detection Configuration
# Confidence threshold for price change detection (0.0 - 1.0)
# Higher values = more strict detection, fewer false positives
# Lower values = more lenient detection, may include borderline cases
# Recommended: 0.75 (balanced) | 0.85 (strict) | 0.65 (lenient)
PRICE_CHANGE_CONFIDENCE_THRESHOLD=0.75

# Session security (optional - will be auto-generated if not provided)
SESSION_SECRET=your-session-secret-key

# Epicor ERP API Configuration
EPICOR_BASE_URL=https://your-epicor-server.com/EpicorData/api/v1
EPICOR_API_KEY=your-epicor-api-key-here
EPICOR_BEARER_TOKEN=your-epicor-bearer-token-here
EPICOR_COMPANY_ID=your-company-id
EPICOR_DEFAULT_PRICE_LIST=UNA1

# Epicor OAuth Configuration (for auto-token generation)
EPICOR_AUTO_TOKEN=false
EPICOR_CLIENT_ID=f4471628-2e91-4a29-bdac-0aa6e4dad31f
EPICOR_CLIENT_SECRET=your-epicor-client-secret
EPICOR_USERNAME=your-epicor-username@domain.com
EPICOR_PASSWORD=your-epicor-password

# NOTE: WEBHOOK_URL is no longer needed - using delta queries instead